<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

	<it.cnr.chlcam.NumberPickerDialogPreference
        android:defaultValue="1"
        android:key="number_pixel"
        android:dialogMessage="Please select the number of pixels:"
        android:max="3721"     
        android:title="Total number of pixel" />

	<CheckBoxPreference
        android:defaultValue="false"
        android:key="average"
        android:summary="@string/pref_description_average"
        android:title="@string/pref_title_average" />


    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="-1"
        android:key="chlorophyll_content"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_chlorophyll_content" />
     
</PreferenceScreen>
